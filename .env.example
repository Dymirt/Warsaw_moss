# Public origin of the separately deployed FastAPI service (no trailing slash).
# This value is compiled into the browser bundle and must never contain a secret.
VITE_API_BASE_URL=http://127.0.0.1:8000
